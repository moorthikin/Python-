#Type casting - converting a variable to one data type to another data type str(),int(),float(),bool()

name = "Adam"
age = 25
gpa = 65.8
is_student = True

# #convert int to float
# gpa = int(gpa)
# print(gpa)
#
# #Convert the int
# age = float(age)
# print(age)
#
# #When we convert the String into boolean if the strings contain the words it will return true if string is empty it is returned false
# name = bool(name)
# print(name)

#convert the int to string
#when we do arithmetic operation in strings doesn't applicable also we should only concortinate the strings only.
age = str(age)
age += "1"
print(age)

